
  # Konflux Website Refresh

  This is a code bundle for Konflux Website Refresh. The original project is available at https://www.figma.com/design/zbiCkeYEmB9CPChFWSDUdK/Konflux-Website-Refresh.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  